// if collection size exceeds this limit, no collection will be created
// from models
const collectionSizeLimit = {
  items: 10,
  users: 999,
  cats: 5,
  filters: 3
}

export default collectionSizeLimit;